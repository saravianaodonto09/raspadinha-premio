<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Raspadinha Cafuné</title>

<style>
body{
    margin:0;
    font-family:Arial, sans-serif;
    background:linear-gradient(135deg,#6a11cb,#2575fc);
    display:flex;
    justify-content:center;
    align-items:center;
    height:100vh;
}

.card{
    width:350px;
    max-width:90%;
    background:#fff;
    border-radius:20px;
    padding:20px;
    text-align:center;
    box-shadow:0 10px 30px rgba(0,0,0,.2);
}

h1{
    color:#333;
}

.scratch-container{
    position:relative;
    width:300px;
    height:180px;
    margin:20px auto;
}

.prize{
    position:absolute;
    inset:0;
    display:flex;
    align-items:center;
    justify-content:center;
    text-align:center;
    background:#fff7cc;
    border:3px dashed #f4b400;
    border-radius:15px;
    font-size:18px;
    font-weight:bold;
    color:#d35400;
    padding:15px;
}

canvas{
    position:absolute;
    inset:0;
    border-radius:15px;
    cursor:pointer;
}
</style>
</head>

<body>

<div class="card">
    <h1>🎉 Raspadinha Especial 🎉</h1>

    <div class="scratch-container">
        <div class="prize">
            🎉 Parabéns! 🎉<br><br>

            Você ganhou um<br>
            <strong>cafuné antiestresse virtual</strong> 💆‍♂️💆‍♀️<br><br>

            Caso persistam os sintomas,<br>
            retire a versão presencial<br>
            com o código:<br><br>

            <span style="font-size:32px;color:#e74c3c;">
                CaFUN3
            </span>
        </div>

        <canvas id="scratch"></canvas>
    </div>
</div>

<!-- SOM + CONFETE -->
<audio id="vitoria" src="https://www.myinstants.com/media/sounds/success-fanfare-trumpets.mp3"></audio>

<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

<script>
const canvas = document.getElementById("scratch");
const ctx = canvas.getContext("2d");

canvas.width = 300;
canvas.height = 180;

// camada cinza da raspadinha
ctx.fillStyle = "#b0b0b0";
ctx.fillRect(0,0,canvas.width,canvas.height);

ctx.fillStyle = "#888";
ctx.font = "bold 22px Arial";
ctx.textAlign = "center";
ctx.fillText("RASPE AQUI", 150, 95);

ctx.globalCompositeOperation = "destination-out";

let drawing = false;

function scratch(x,y){
    ctx.beginPath();
    ctx.arc(x,y,18,0,Math.PI*2);
    ctx.fill();
}

// 🎉 confete
function soltarConfete(){
    confetti({
        particleCount: 180,
        spread: 90,
        origin: { y: 0.6 }
    });
}

// 🔊 som
function tocarSom(){
    const audio = document.getElementById("vitoria");
    audio.currentTime = 0;
    audio.play();
}

// 📳 vibração
function vibrar(){
    if (navigator.vibrate) {
        navigator.vibrate([200, 100, 200]);
    }
}

// ação de vitória
function vitoria(){
    soltarConfete();
    tocarSom();
    vibrar();
}

// mouse (PC)
canvas.addEventListener("mousedown",()=>drawing=true);

canvas.addEventListener("mouseup",()=>{
    drawing=false;
    vitoria();
});

canvas.addEventListener("mousemove",(e)=>{
    if(!drawing) return;
    const rect = canvas.getBoundingClientRect();
    scratch(e.clientX - rect.left, e.clientY - rect.top);
});

// touch (celular)
canvas.addEventListener("touchstart",()=>drawing=true);

canvas.addEventListener("touchend",()=>{
    drawing=false;
    vitoria();
});

canvas.addEventListener("touchmove",(e)=>{
    e.preventDefault();
    if(!drawing) return;

    const rect = canvas.getBoundingClientRect();
    const touch = e.touches[0];

    scratch(
        touch.clientX - rect.left,
        touch.clientY - rect.top
    );
},{passive:false});

</script>

</body>
</html>